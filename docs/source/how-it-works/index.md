# How It Works

These pages explain what VlConvert does with a chart and how it is built. They apply to every interface.

- {doc}`rendering` covers what users see: the input types, output formats, fonts, network access, and worker model.
- {doc}`architecture` covers the implementation: the embedded JavaScript runtime, vendored libraries, conversion pipeline, Canvas 2D backend, access control, and the crates involved.

```{toctree}
:maxdepth: 1

rendering
architecture
```
